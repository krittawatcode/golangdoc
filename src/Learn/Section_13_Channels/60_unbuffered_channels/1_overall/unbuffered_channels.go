package main

func main() {
	// uncomment to read
	/*
		ch := make(chan int)
		done := make(chan struct{}) // done <- struct{}{}
		sender(ch, done)
		receiver(ch, done)
		doneStatus := <-done
		fmt.Println("Done status : ", doneStatus)
		doneStatus = <-done
		fmt.Println("Done status : ", doneStatus)
		fmt.Println("Main exit")
	*/
}
